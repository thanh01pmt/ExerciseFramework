//
//  Exercise.swift
//  ExerciseFramework
//
//  Created by Tony Pham on 18/11/24.
//

import Foundation

public struct Topic1Exercise {
    public static func sum(a: Int, b: Int) -> Int {
        
        return 0
        
        // Viết lệnh của bạn ở trên
        // Giữ nguyên đoạn lệnh bên dưới
        fatalError("Chưa implement hàm sum")
    }
    public static func multiply(a: Int, b: Int) -> Int {
        
        return a * b
        
        // Viết lệnh của bạn ở trên
        // Giữ nguyên đoạn lệnh bên dưới
        fatalError("Chưa implement hàm multiply")
    }
}
